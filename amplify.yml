version: 1
frontend:
  phases:
    preBuild:
     "react-scripts"
    build:
      "react-scripts build"
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
