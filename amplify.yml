version: 1
frontend:
  phases:
    preBuild:
      - npm install
    build:
      - npm build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
